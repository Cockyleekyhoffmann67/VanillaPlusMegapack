# v2

Includes Enemy Collision Synchronized v2.6 as the seventh pinned gameplay component. Uses the same compiled gameplay resources as the standalone releases and requires the separately installed Bingus Shared Loader v9. Public ZIP and Arsenal names use consistent version formatting.
